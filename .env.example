# Auth0 and server configuration for NestJS Auth0 JWT API
# Copy this file to .env and fill in your Auth0 Dashboard values.

# HTTP port the server listens on (e.g. 6060)
PORT=6060

# Auth0 tenant issuer base URL (no path). Example: https://your-tenant.auth0.com/
# Obtain from Auth0 Dashboard: Tenant Settings -> Domain
AUTH0_ISSUER_BASE_URL=https://your-tenant.auth0.com/

# Auth0 API identifier (audience). Create an API in Auth0 Dashboard and use its Identifier.
AUTH0_AUDIENCE=https://hello-world.example.com

# Allowed CORS origin for browser clients (e.g. your SPA URL)
CLIENT_ORIGIN_URL=http://localhost:4040
